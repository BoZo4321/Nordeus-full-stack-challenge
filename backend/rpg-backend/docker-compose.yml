services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: rpgdb
      POSTGRES_USER: rpg
      POSTGRES_PASSWORD: rpg
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
